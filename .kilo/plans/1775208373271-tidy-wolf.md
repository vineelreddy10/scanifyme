# Plan: Create Test Kilo DocType in scanifyme app

## Summary
Create a new DocType called "Test Kilo" in the existing scanifyme Frappe app with 2 fields:
- **title** (Data) - a short text field
- **description** (Text) - a multi-line text field

Full Desk-style structure with 5 files: JSON schema, Python controller, client-side JS, test file, and `__init__.py`.

## Steps

### 1. Create DocType folder and JSON definition
- Create `scanifyme/doctype/test_kilo/test_kilo.json` with:
  - DocType name: "Test Kilo"
  - Module: "Scanifyme"
  - 2 fields: `title` (Data), `description` (Text)
  - Permissions: System Manager role with full CRUD
  - `title_field` set to "title"

### 2. Create Python controller
- Create `scanifyme/doctype/test_kilo/test_kilo.py` with `TestKilo(Document)` class

### 3. Create client-side JS controller
- Create `scanifyme/doctype/test_kilo/test_kilo.js` with:
  - Standard `frappe.ui.form.on('Test Kilo', {...})` scaffold
  - `refresh` event handler (standard Desk pattern)

### 4. Create test file
- Create `scanifyme/doctype/test_kilo/test_test_kilo.py` with:
  - Standard Frappe test class inheriting `unittest.TestCase` or `FrappeTestCase`
  - Basic test that creates a Test Kilo document and verifies fields

### 5. Create `__init__.py`
- Create `scanifyme/doctype/test_kilo/__init__.py` (empty)

### 6. Run bench migrate
- Run `bench --site test.localhost migrate` to register the DocType

## Files to create
- `scanifyme/doctype/test_kilo/__init__.py`
- `scanifyme/doctype/test_kilo/test_kilo.json`
- `scanifyme/doctype/test_kilo/test_kilo.py`
- `scanifyme/doctype/test_kilo/test_kilo.js`
- `scanifyme/doctype/test_kilo/test_test_kilo.py`

## Notes
- Site: `test.localhost` (already identified)
- Bench root: `/home/vineelreddykamireddy/vibecoding_apps/staging_scan`
